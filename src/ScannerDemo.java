import java.util.*;

public class ScannerDemo {
    public static void main(String[] args){
        //input
        Scanner sc = new Scanner(System.in);
        String name = sc.next(); //prints only 1 token of any line
        // String name = sc.nextLine(); // nextLine prints all tokens, means full line
       /* nextInt() used to get integer input in scanner class
        nextFloat() to get float input , nextDouble(), nextLong() etc */
        System.out.println(name);

        /* ASSIGNMENT Q1. TAKE 2 VARIABLES a and b and print their sum using scanner input.
        sol code ------  */
        // Scanner sc = new Scanner(System.in);
        // int a = sc.nextInt();
        // int b = sc.nextInt();
        // int sum = a+b;
        // System.out.println(sum);
    }
}
